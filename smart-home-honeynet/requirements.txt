# Runtime: the core honeynet runs on the Python standard library only.
# PyYAML is optional — it lets you edit config/detection_rules.yaml as YAML
# instead of JSON.
PyYAML>=6.0

# Development / testing
pytest>=7.4
pytest-cov>=4.1
